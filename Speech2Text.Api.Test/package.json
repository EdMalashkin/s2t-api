{
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "supertest": "^6.3.3"
  },
  "scripts": {
    "test": "mocha --timeout 10000 test.js && mocha --reporter json > test-results.json"
  }
}
